export const pdfService = {
  exportToPDF(content: string, filename: string): void {
    console.log(`Exporting ${filename} to PDF...`);
    // Implementation for generating and triggering PDF download
  }
};
